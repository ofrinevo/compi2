
class BoolCheck { 

	static void main (string[] args) { 

		int x = 4 ; 
		int y = 23 ; 

		boolean p ; 
		
		p =    ( ( x > 0     ) || ( -y < (5-3)    ) || ( (y%x) != x    ) ) 
		    && ( ( (y/x) > 3   ) || ( (x-y) < (5-3)   ) || ( (y%x) != x    ) ) 
		    && ( ( (x+2*y) > 3 ) || ( (x-y) < 5     ) || ( x == y      ) ) 
			&& ( ( x == x    ) || ( y < -1      ) || ( y != y      ) )
			&& ( ( (1-x) > 3   ) || ( (1-y) < (5/8)   ) || ( (x%x) == (y-y)  ) ) 
			&& ( ( (x*x) > 25  ) || ( (-y*y) < (-x*x) ) || ( (y/x) != 1    ) ) 
			&& ( ( (y/x/x) > 2 ) || ( ((y/x)/x) < 3 ) || ( (y/(x/x)) < x ) ) 
			&& ( ( x == 0    ) || ( y == 0      ) || ( (y-x) > 0     ) ) ; 

		// p supposed to be true
		
		Library.printb(p) ;
		Library.println("");

		p = (x<3) || (y>24);
		// p should be false
		Library.printb(p);
		Library.println("");

		p = (x>3) || (y>24);
		// p should be true
		Library.printb(p);
		Library.println("");

	}

}
